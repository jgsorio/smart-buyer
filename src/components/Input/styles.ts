import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
       width: '100%',
       paddingHorizontal: 10,
       paddingVertical: 10,
       height: 40,
       borderWidth: 0.7,
       marginTop: 14,
       borderRadius: 8,
       color: '#000',
    }
});
